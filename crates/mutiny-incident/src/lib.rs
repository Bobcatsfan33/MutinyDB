//! The M4 incident host: dev-only composition of storage, bridge, compute, semantic, and trust
//! for the frozen incident corpus. Not the supported `mutinyd` binary (that is M6); nothing here
//! changes any component's release admission.

pub mod corpus;
pub mod host;
