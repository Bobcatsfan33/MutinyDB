//! Forked standing state (M5). The spike in `tests/m5_spike.rs` ran first, against the criteria
//! MD-5 committed before it; this library implements the path the verdict selected.

pub const PLACEHOLDER_UNTIL_VERDICT: &str =
    "the M5 spike decides before production code lands here (MD-5)";
